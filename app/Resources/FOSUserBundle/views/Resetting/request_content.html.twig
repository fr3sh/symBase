{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_resetting_send_email'), 'attr': {'class': 'fos_user_resetting_request'}}) }}
{#<form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">#}

    <div id="legend">
        <legend class="">Reset Hasła</legend>
    </div>


    <div class="form-group row">
        <!-- Username -->
        <label class="col-sm-3 form-control-label" for="username">{{ 'resetting.request.username'|trans }}</label>
        <div class="col-sm-5">
            <input class="form-control"  type="text" id="username" name="username" required="required" />
            {% if invalid_username is defined %}
                <p>{{ 'resetting.request.invalid_username'|trans({'%username%': invalid_username}) }}</p>
            {% endif %}
        </div>
    </div>


    <div class="form-group row">
            
  {% block captcha_widget %}
      {{ form_label(form.captcha, null,{'label_attr': {'class': 'col-sm-3 form-control-label'}} ) }}
      <div class="col-sm-5">
          
{% spaceless %}
  {#  <img src="{{ form.captcha.vars.captcha_code }}" title="captcha" width="{{ form.captcha.vars.captcha_width }}" height="{{ form.captcha.vars.captcha_height }}" />#}
    {{ form_widget(form.captcha) }}
   
{% endspaceless %}
{% endblock %}
        
      
      </div>
    </div>     
        
        
   



    <div class="form-group row">
        <!-- Button -->
        <div class="col-sm-5">


            <input class="btn btn-success" type="submit" value="{{ 'resetting.request.submit'|trans }}" />
        </div>
    </div>



{{ form_end(form) }}
{#</form>#}

            {{dump()}}